# MagicPocket 项目发展规划文档

## 项目概述

MagicPocket 是一个基于 Vue 3 的低代码平台，旨在提供快速、高效、AI 友好的页面开发体验。项目的核心目标是让开发者能够通过可视化拖拽方式快速构建页面，同时保持代码的可控性和可扩展性。

## 当前项目结构

```
magicPocket/
├── src/
│   ├── main/                 # 主进程代码
│   ├── preload/              # 预加载脚本
│   └── renderer/             # 渲染进程代码
│       ├── src/
│       │   ├── api/          # API 接口
│       │   ├── components/   # 公共组件
│       │   ├── core/         # 核心功能模块
│       │   ├── internal/     # 内部组件库
│       │   ├── packages/     # 可视化组件库
│       │   ├── stores/       # 状态管理
│       │   ├── type/         # 类型定义
│       │   ├── utils/        # 工具函数
│       │   ├── views/        # 视图组件
│       │   └── main.ts       # 入口文件
│       └── package.json
├── README.md
├── package.json
└── CLAUDE.md                 # 本文件 - 项目发展规划文档
```

## 当前问题分析

### 1. 性能问题
- **JSON 整体响应式监听**: `RenderEngine.jsx` 中使用了深层监听（`{ deep: true }`），当 JSON 结构复杂时会导致页面卡顿
- **不必要的渲染触发**: 每次 JSON 变化都会触发整个组件树的重新渲染
- **历史记录深拷贝**: `useCanvasOperation.ts` 中的 `deepClone` 操作可能成为性能瓶颈

### 2. JSON 结构问题
- **编辑与渲染混用**: 当前编辑和渲染使用的同一套 JSON 结构，包含大量编辑器专用属性
- **渲染性能**: 渲染时仍需处理所有编辑器专用属性，增加了不必要的计算

### 3. 组件架构
- **动态组件加载**: 通过 `createAsyncComponent` 加载组件，可能存在异步加载性能问题
- **层级嵌套**: 多层组件嵌套可能导致性能下降

## 改进方案

### 1. 分离编辑与渲染 JSON 结构
正如需求中提到的，应该分离两种用途的 JSON：

- **编辑用 JSON**: 包含完整的控制器信息、属性配置等
- **渲染用 JSON**: 只包含必要的结构和数据，去除控制器专用属性

### 2. 优化渲染性能
- **减少深层监听**: 避免对整个 JSON 对象进行深层监听
- **局部更新**: 只更新发生变化的组件部分
- **虚拟滚动**: 对于大量组件的场景使用虚拟滚动

### 3. 组件懒加载和缓存
- **组件缓存**: 对已加载的组件进行缓存
- **渐进式渲染**: 采用懒加载方式逐步渲染组件

## 后续开发计划

### 第一阶段：性能优化（当前）

**目标**: 解决核心性能问题，提升用户体验
- [x] 分析当前架构和性能瓶颈
- [ ] 实现编辑 JSON 与渲染 JSON 的分离
- [ ] 优化渲染引擎，减少不必要的重渲染
- [ ] 实现组件缓存机制
- [ ] 重构历史记录机制，避免深拷贝性能问题

### 第二阶段：架构优化（下一步）

**目标**: 完善架构设计，提升可维护性
- [ ] 完善组件注册和加载机制
- [ ] 实现虚拟滚动以支持大页面
- [ ] 优化状态管理，引入更高效的状态更新机制
- [ ] 完善组件库的标准化接口

### 第三阶段：AI 友好设计（长期）

**目标**: 为 AI 辅助开发做好准备
- [ ] 标准化组件元数据结构
- [ ] 提供清晰的语义化标签
- [ ] 设计易于理解和学习的 JSON 协议
- [ ] 实现组件智能推荐机制

### 第四阶段：功能增强（未来）

**目标**: 增强平台功能，提升竞争力
- [ ] 实现主题定制功能
- [ ] 增加更多组件库支持
- [ ] 完善权限管理和协作功能
- [ ] 实现高级数据绑定和联动

## 技术架构优化

### 分层设计
```
┌─────────────────┐
│   AI 层         │ ← AI 理解、推荐、辅助
├─────────────────┤
│   应用层        │ ← 用户界面、操作逻辑
├─────────────────┤
│   渲染层        │ ← 渲染引擎、组件管理
├─────────────────┤
│   数据层        │ ← JSON 协议、状态管理
├─────────────────┤
│   组件库层      │ ← 标准化组件、第三方库
└─────────────────┘
```

### JSON 协议优化

#### 编辑用 JSON (EditorJSON)
```json
{
  "type": "page",
  "title": "页面",
  "metadata": {
    "editMode": true,
    "controls": ["drag", "resize", "configure"]
  },
  "children": [
    {
      "type": "Form",
      "key": "form-1",
      "componentName": "表单",
      "props": {
        "formProps": { /* 表单配置 */ },
        "formItemProps": { /* 表单项配置 */ }
      },
      "controllers": {
        "visible": true,
        "editable": true
      }
    }
  ]
}
```

#### 渲染用 JSON (RenderJSON)
```json
{
  "type": "page",
  "children": [
    {
      "type": "Form",
      "key": "form-1",
      "props": {
        "span": 12,
        "gutter": 20
      },
      "children": [
        {
          "type": "FormItem",
          "key": "input-1",
          "props": {
            "label": "姓名",
            "name": "name",
            "required": true
          }
        }
      ]
    }
  ]
}
```

## 组件标准化规范

### 组件接口定义
```typescript
interface LowCodeComponent {
  // 组件基本信息
  componentName: string;      // 组件名称
  type: string;              // 组件类型标识
  icon?: string;             // 图标
  group?: string;            // 分组

  // 渲染信息
  npm?: NodeNpmInfo;         // npm 信息
  props: Record<string, any>; // 属性配置

  // 控制器信息（编辑态）
  controllers?: ControllerConfig[]; // 控制器配置
}
```

## 文档目录

```
docs/
├── architecture/             # 架构设计文档
│   ├── overview.md         # 架构概览
│   ├── rendering.md        # 渲染引擎设计
│   └── protocol.md         # JSON 协议规范
├── components/             # 组件库文档
│   ├── guidelines.md       # 组件开发指南
│   └── standards.md        # 组件标准规范
└── performance/            # 性能优化文档
    ├── issues.md          # 性能问题分析
    └── solutions.md       # 优化方案
```

## 开发原则

1. **性能优先**: 始终将性能作为第一考虑因素
2. **AI 友好**: 设计易于 AI 理解和操作的接口
3. **组件复用**: 高度组件化，便于复用
4. **标准化**: 统一接口和协议标准
5. **可扩展**: 保持良好的扩展性

## 成功指标

- [ ] 编辑页面 JSON 结构时不再出现卡顿现象
- [ ] 渲染性能提升 50% 以上
- [ ] 组件复用率达到 80% 以上
- [ ] JSON 协议清晰易懂，便于 AI 学习
- [ ] 支持大规模页面渲染（1000+ 组件）

## 时间规划

- **短期 (1-2周)**: 完成性能问题分析和初步优化
- **中期 (3-6周)**: 完成分离 JSON 结构和渲染引擎优化
- **长期 (2-3个月)**: 完成整体架构优化和 AI 集成准备

---
*最后更新时间: 2026-02-09*